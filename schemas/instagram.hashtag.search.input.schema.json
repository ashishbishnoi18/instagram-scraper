{
  "type": "object",
  "properties": {
    "hashtag": {
      "type": "string",
      "description": "Hashtag to search",
      "example": "photography"
    },
    "limit": {
      "type": "integer",
      "description": "Max results to return"
    }
  },
  "required": [
    "hashtag"
  ]
}
